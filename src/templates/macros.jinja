{# Provide some common macros that can be used on *any* jinja template,
regardless of type (css, html, etc) #}

{% macro include_inline(page) %}
{% do page_info.srcdeps.add(page) %}
{{ page.contents }}
{% endmacro %}
